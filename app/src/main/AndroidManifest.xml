<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.digitalgarden.gameswap"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission-sdk-23 android:name="android.permission.CAMERA" />
    <uses-permission-sdk-23 android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.com.digitalgarden.gameswap.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

        <activity
            android:name=".activity.ActivityHome"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.ActivityCreateAccount"
            android:label="Create Account"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity
            android:name=".activity.ActivityProductList"
            android:label="Product Page"/>

        <activity
            android:name=".activity.ActivityProduct"
            android:label="Product"/>

        <activity
            android:name=".activity.ActivityLocations"
            android:label="Locations"/>

        <activity
            android:name=".activity.ActivitySettings"
            android:label="Settings"/>

        <activity
            android:name=".activity.ActivityAbout"
            android:label="About"/>

        <activity
            android:name=".activity.ActivityCreateListing"
            android:label="Create Post"/>

        <activity
            android:name=".activity.ActivityMyPosts"
            android:label="My Posts"/>

        <activity
            android:name=".activity.ActivityMyPost"
            android:label="My Post"/>

        <activity
            android:name=".activity.ActivitySearch"
            android:label="Search"/>

        <activity
            android:name=".activity.ActivityPost"
            android:label="Post"/>

        <activity
            android:name=".activity.ActivityMock"
            android:label="Mock"/>

    </application>

</manifest>